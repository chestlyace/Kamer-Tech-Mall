# 🏗️ Architecture Overview

## How Login & Registration Pages Connect to Backend

```
┌─────────────────────────────────────────────────────────────┐
│                    USER BROWSER                              │
└────────────┬────────────────────────────────────────────────┘
             │
             │  HTTP Requests
             ↓
┌─────────────────────────────────────────────────────────────┐
│                  EXPRESS SERVER (server.js)                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Middleware Stack:                                    │  │
│  │  → Body Parser (parse form data)                     │  │
│  │  → Cookie Parser (handle cookies)                    │  │
│  │  → Express Session (manage sessions)                 │  │
│  │  → Flash Messages (user feedback)                    │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Routes:                                              │  │
│  │  → /auth/* (authentication routes)                   │  │
│  │  → /seller/* (protected seller routes)               │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────┬───────────────────────────────────────────────┘
              │
              ↓
┌─────────────────────────────────────────────────────────────┐
│                   ROUTE HANDLERS                             │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  routes/auth.js                                      │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ GET /auth/register                            │  │   │
│  │  │  → Render registration form (EJS)             │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ POST /auth/register                           │  │   │
│  │  │  1. Validate input (express-validator)        │  │   │
│  │  │  2. Check for existing user                   │  │   │
│  │  │  3. Hash password (bcrypt)                    │  │   │
│  │  │  4. Create seller in database                 │  │   │
│  │  │  5. Redirect to login                         │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ GET /auth/login                               │  │   │
│  │  │  → Render login form (EJS)                    │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ POST /auth/login                              │  │   │
│  │  │  1. Validate input                            │  │   │
│  │  │  2. Find user by email                        │  │   │
│  │  │  3. Verify password (bcrypt.compare)          │  │   │
│  │  │  4. Create session                            │  │   │
│  │  │  5. Redirect to dashboard                     │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  routes/seller.js                                    │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ GET /seller/dashboard (Protected)             │  │   │
│  │  │  → Check authentication (middleware)          │  │   │
│  │  │  → Fetch seller data                          │  │   │
│  │  │  → Render dashboard (EJS)                     │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ GET /seller/profile (Protected)               │  │   │
│  │  │  → Render profile page                        │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ POST /seller/profile (Protected)              │  │   │
│  │  │  → Update seller information                  │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
└──────────────┬──────────────────────────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────────────────────────┐
│              AUTHENTICATION MIDDLEWARE                       │
│              (middleware/auth.js)                            │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  isAuthenticated()                                  │    │
│  │   → Check if session.sellerId exists               │    │
│  │   → If yes: allow access                           │    │
│  │   → If no: redirect to login                       │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  isGuest()                                          │    │
│  │   → Check if user is NOT logged in                 │    │
│  │   → Prevent logged-in users from seeing login      │    │
│  └────────────────────────────────────────────────────┘    │
└──────────────┬──────────────────────────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────────────────────────┐
│                    DATA MODELS                               │
│                    (models/Seller.js)                        │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Seller.create(data)                                │    │
│  │   → Hash password with bcrypt                       │    │
│  │   → Insert into database                            │    │
│  │   → Return seller object                            │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Seller.findByEmail(email)                          │    │
│  │   → Query database by email                         │    │
│  │   → Return seller or null                           │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Seller.verifyPassword(plain, hash)                 │    │
│  │   → Compare using bcrypt                            │    │
│  │   → Return boolean                                  │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Seller.update(id, data)                            │    │
│  │   → Update seller record                            │    │
│  │   → Return updated seller                           │    │
│  └────────────────────────────────────────────────────┘    │
└──────────────┬──────────────────────────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────────────────────────┐
│              DATABASE ABSTRACTION LAYER                      │
│              (config/database.js)                            │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Supports Both:                                     │    │
│  │   → MySQL (local development)                      │    │
│  │   → Supabase (production)                          │    │
│  │                                                     │    │
│  │  Provides unified interface:                       │    │
│  │   → database.query(sql, params)                    │    │
│  │   → database.getClient()                           │    │
│  └────────────────────────────────────────────────────┘    │
└──────────────┬──────────────────────────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────────────────────────┐
│                      DATABASE                                │
│                                                              │
│  ┌─────────────────────┐    ┌─────────────────────────┐    │
│  │  MySQL (Local)      │ OR │  Supabase (Production)  │    │
│  │                     │    │                         │    │
│  │  Tables:            │    │  Tables:                │    │
│  │  → sellers          │    │  → sellers              │    │
│  │  → seller_sessions  │    │  → seller_sessions      │    │
│  └─────────────────────┘    └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow Examples

### Registration Flow

```
1. User fills form at /auth/register
   ↓
2. Form submits POST to /auth/register
   ↓
3. routes/auth.js receives request
   ↓
4. express-validator validates input
   ↓
5. Check if username exists (Seller.findByUsername)
   ↓
6. Check if email exists (Seller.findByEmail)
   ↓
7. Hash password with bcrypt (10 rounds)
   ↓
8. Seller.create() saves to database
   ↓
9. Flash success message
   ↓
10. Redirect to /auth/login
```

### Login Flow

```
1. User enters credentials at /auth/login
   ↓
2. Form submits POST to /auth/login
   ↓
3. routes/auth.js receives request
   ↓
4. Find seller by email (Seller.findByEmail)
   ↓
5. Verify password with bcrypt.compare()
   ↓
6. Check account status (not suspended)
   ↓
7. Create session:
   - req.session.sellerId
   - req.session.sellerUsername
   - req.session.sellerEmail
   - req.session.sellerStatus
   ↓
8. Flash success message
   ↓
9. Redirect to /seller/dashboard
```

### Accessing Protected Routes

```
1. User requests /seller/dashboard
   ↓
2. isAuthenticated middleware checks session
   ↓
3. If no session → redirect to /auth/login
   ↓
4. If session exists → allow access
   ↓
5. Fetch seller data (Seller.findById)
   ↓
6. Render dashboard with seller data
```

## 🎨 View Templates (EJS)

```
views/
├── auth/
│   ├── login.ejs           → Login form
│   ├── register.ejs        → Registration form
│   └── suspended.ejs       → Suspended account page
├── seller/
│   ├── dashboard.ejs       → Main dashboard
│   └── profile.ejs         → Profile editor
├── home.ejs                → Landing page
├── 404.ejs                 → Not found
└── error.ejs               → Error page
```

Each template:
- Uses Tailwind CSS for styling
- Includes Font Awesome icons
- Displays flash messages
- Has responsive navigation
- Shows user session state

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│  1. Input Validation                    │
│     → express-validator                 │
│     → Server-side validation            │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│  2. Password Security                   │
│     → bcrypt hashing (10 rounds)        │
│     → Never store plain passwords       │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│  3. Session Management                  │
│     → express-session                   │
│     → HTTP-only cookies                 │
│     → Secure cookies (production)       │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│  4. SQL Injection Prevention            │
│     → Prepared statements               │
│     → Parameterized queries             │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│  5. XSS Protection                      │
│     → EJS automatic escaping            │
│     → Content Security Policy           │
└─────────────────────────────────────────┘
┌─────────────────────────────────────────┐
│  6. Authentication Middleware           │
│     → Route protection                  │
│     → Session verification              │
└─────────────────────────────────────────┘
```

## 📁 File Connections

```
server.js
  ├─→ requires config/database.js
  ├─→ requires routes/auth.js
  ├─→ requires routes/seller.js
  └─→ serves views/*.ejs

routes/auth.js
  ├─→ requires models/Seller.js
  ├─→ requires middleware/auth.js
  └─→ renders views/auth/*.ejs

routes/seller.js
  ├─→ requires models/Seller.js
  ├─→ requires middleware/auth.js
  └─→ renders views/seller/*.ejs

models/Seller.js
  └─→ requires config/database.js

middleware/auth.js
  └─→ uses req.session
```

## 🚀 Complete Request Cycle

```
User clicks "Register"
  ↓
Browser: GET /auth/register
  ↓
Server: routes/auth.js → GET handler
  ↓
Server: Render views/auth/register.ejs
  ↓
Browser: Display registration form
  ↓
User fills form and submits
  ↓
Browser: POST /auth/register with form data
  ↓
Server: routes/auth.js → POST handler
  ↓
Server: Validate input with express-validator
  ↓
Server: models/Seller.create()
  ↓
Database: INSERT INTO sellers
  ↓
Server: Set flash message
  ↓
Server: Redirect to /auth/login
  ↓
Browser: Display login page with success message
```

## 💾 Session Storage

```
When user logs in:

req.session = {
  sellerId: 1,
  sellerUsername: "testuser",
  sellerEmail: "test@example.com",
  sellerStatus: "pending",
  cookie: {
    maxAge: 86400000,  // 24 hours
    httpOnly: true,
    secure: false      // true in production
  }
}

This session is:
→ Stored server-side
→ Referenced by session cookie
→ Available across all requests
→ Checked by authentication middleware
```

## ✅ Everything is Connected!

All components work together seamlessly:
- ✅ Forms submit to backend routes
- ✅ Routes call model methods
- ✅ Models interact with database
- ✅ Middleware protects routes
- ✅ Sessions track authentication
- ✅ Views display dynamic data
- ✅ Flash messages provide feedback

Ready to test? Run: `./start.sh` or `npm run dev`

